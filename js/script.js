const stasiData = {
    'margo-agung': {
        title: 'Gereja Paroki Margo Agung',
        patron: 'Santo Andreas Rasul',
        patronFeast: '30 November',
        patronRole: 'Rasul Pertama & Saudara Simon Petrus',
        patronBio: 'Santo Andreas adalah seorang nelayan asal Betsaida dan saudara kandung Simon Petrus. Ia merupakan murid pertama yang mengikuti Yesus setelah mendengar kesaksian Yohanes Pembaptis. Andreas memiliki keteladanan dalam membawa sesama kepada Kristus—mengajak Petrus dan mempertemukan anak pembawa lima roti serta dua ikan kepada Yesus. Ia mewartakan Injil hingga Yunani dan wafat sebagai martir disalibkan pada kayu silang berbentuk huruf "X" (Salib Santo Andreas).',
        address: 'Jl. Margo Agung, Marga Kaya, Kec. Jati Agung, Kab. Lampung Selatan, Lampung 35365',
        gmapsUrl: 'https://www.google.com/maps/search/?api=1&query=Gereja+Katolik+Santo+Andreas+Rasul+Marga+Agung+Lampung+Selatan',
        image: 'assets/img/stasi/marga-agung.jpg',
        history: 'Gereja ini terletak di desa Margo Agung, Jati Agung. Terbentuk akibat transmigrasi korban letusan Gunung Merapi tahun 1961 dari Magelang, Jawa Tengah. Rombongan pertama tiba 27 Juni 1961. Cikal bakal gereja dimulai dari 9 KK Katolik perdana di Marga Kaya. Gedung gereja pertama (semi permanen) selesai tahun 1962-1963 di Blok C dan diresmikan oleh Bapa Uskup Hermelink Gentiaras SCJ pada 27 Juni 1964.',
        chairmen: ['Yohakim Noto Siswoyo', 'Yohakim Sosrowasito', 'Petrus Siswosukarto', 'Fx. Edi Suwarno', 'Paulus Sutikno', 'Yohanes Supandi', 'Petrus Sukamto', 'Fx. Heri Hendarto', 'Andreas Kamajaya (2024-Sekarang)']
    },
    'marga-lestari': {
        title: 'Stasi Marga Lestari',
        patron: 'Santa Maria',
        patronFeast: '1 Januari & 15 Agustus',
        patronRole: 'Bunda Yesus Kristus & Teladan Ketaatan Iman',
        patronBio: 'Santa Perawan Maria adalah Ibunda Yesus Kristus sekaligus teladan ketaatan, kerendahan hati, dan iman yang murni bagi seluruh umat beriman. Melalui kerelaan hatinya mengucap "Fiat" (Terjadilah padaku menurut perkataan-Mu), karya keselamatan Allah terwujud. Maria setia menyertai Yesus dari palungan hingga kaki salib di Golgota. Di Stasi Marga Lestari, nama pelindung Santa Maria terinspirasi dari Sendang Sono.',
        address: 'Desa Marga Lestari, Kec. Jati Agung, Kab. Lampung Selatan',
        gmapsUrl: 'https://www.google.com/maps/search/?api=1&query=Gereja+Katolik+Santa+Maria+Marga+Lestari+Jati+Agung',
        image: 'assets/img/stasi/marga-lestari.jpg',
        history: 'Berbeda dengan Margo Agung, umat di sini bukan dari transmigrasi Merapi, melainkan daerah bukaan tahun 1962. Awalnya bagian dari Gedung Meneng I. Tokoh perintisnya adalah Bapak Ayub dan Bapak Aliun. Gereja pertama dibangun tahun 1968 dengan semangat Persatuan Konten, hasil gotong royong umat Katolik dan warga Muslim setempat. Nama pelindung Santa Maria terinspirasi dari Sendang Sono, ditandai dengan penanaman potongan jari patung Bunda Maria Sendang Sono di bawah altar gereja lama.',
        chairmen: ['Kirin Siswo Soemarto', 'Ignasius Sis Mardji', 'Robertus Soeryo Martono', 'Yusuf Basuki', 'Gabriel Irawan Edi Soeyono', 'Robertus Yunarto', 'Wiryo Suprayogi (2005)', 'Robertus Yunarto (2008)', 'Antonius Hadi Susanto (2011)', 'Yohanes Seh Hadi (2014)', 'Hariyadi (2017)', 'Yohanes Seh Hadi (2020)', 'Antonius Imam S (2023)', 'Antonius Suhartono (2025)']
    },
    'sindang-sari': {
        title: 'Stasi Sindangsari',
        patron: 'Santo Stefanus',
        patronFeast: '26 Desember',
        patronRole: 'Protomartir (Martir Pertama) & Diakon Gereja',
        patronBio: 'Santo Stefanus adalah protomartir (martir pertama dalam sejarah Gereja) dan satu dari tujuh diakon pertama yang dipilih para rasul di Yerusalem. Dipenuhi karunia Roh Kudus, ia berkhotbah dengan hikmat yang luar biasa. Saat dirajam batu hingga wafat, ia meneladani Kristus dengan berseru mendoakan musuhnya: "Tuhan, janganlah tanggungkan dosa ini kepada mereka!"—teladan luhur pengampunan tanpa batas.',
        address: 'Dusun Umbul Kapuk, Desa Sindangsari, Kec. Tanjung Bintang, Kab. Lampung Selatan',
        gmapsUrl: 'https://www.google.com/maps/search/?api=1&query=Gereja+Katolik+Santo+Stefanus+Sindang+Sari+Tanjung+Bintang',
        image: 'assets/img/stasi/sindang-sari.jpg',
        history: '<strong>Awal Kedatangan Tokoh Perintis (1949–1950)</strong><br>Gereja Santo Stefanus telah berdiri sejak tahun 1967. Kehadiran gereja ini tidak lepas dari peran seorang tokoh Katolik dari Jawa, yaitu Bapak Laurensius Sastro Wirono Sukapjo, berasal dari Piton, Kulon Progo, DIY Yogyakarta. Pada tahun 1949, beliau pergi ke Lampung dan menetap di Desa Karang Anyar, Kecamatan Jati Agung. Setahun kemudian, pada tahun 1950, beliau pindah ke Way Rilau (kini Desa Sindangsari, Kecamatan Tanjung Bintang), yang saat ini merupakan kantor perkebunan karet AVD VII milik PT. Nusantara VII. Bapak Sastro dikenal sebagai tokoh yang handal dan banyak berhasil menarik orang untuk masuk ke dalam Gereja Katolik.<br><br><strong>Perintisan Iman dan Pendirian Gereja (1950–1967)</strong><br>Pada masa itu, daerah Way Rilau belum memiliki bangunan gereja. Misa dan ibadat diadakan di rumah-rumah umat. Romo yang sering melayani adalah Romo Kam, seorang misionaris dari Belanda. Secara administratif, daerah ini masih masuk wilayah pelayanan Paroki Katedral Kristus Raja, Tanjung Karang. Pada mulanya, perayaan Ekaristi hanya dihadiri 3–5 orang. Seiring bertambahnya umat pada tahun 1960-an, pada tahun 1967 didirikanlah gereja atas inisiatif Bapak Sastro Wirono, dibantu Bapak Sunar (alm). Lokasi di Sindangsari (Dusun Umbul Kapuk) tidak berpindah hingga saat ini, dan bangunan aslinya masih berdiri kokoh.<br><br><strong>Perkembangan Umat dan Pelayanan Saat Ini</strong><br>Dari hanya satu Keluarga Katolik pada tahun 1950, kini pada tahun 2025, Stasi Santo Stefanus Sindangsari telah berkembang menjadi 38 Kepala Keluarga atau 87 jiwa. Stasi ini kini masuk ke dalam wilayah pelayanan Paroki Santo Andreas Rasul, Margo Agung.',
        chairmen: ['Laurensius Sastro Wirono Sukapjo (Perintis, 1950–1967)']
    },
    'rejomulyo': {
        title: 'Stasi Rejomulyo',
        patron: 'Tritunggal Mahakudus',
        patronFeast: 'Minggu I stl. Pentakosta (Mei/Juni)',
        patronRole: 'Misteri Sentral Iman Katolik (Bapa, Putra, & Roh Kudus)',
        patronBio: 'Tritunggal Mahakudus adalah misteri iman sentral Gereja Katolik: Satu Allah dalam Tiga Pribadi Ilahi yang sehakikat—Allah Bapa Sang Pencipta, Allah Putra Sang Penebus, dan Allah Roh Kudus Sang Pengudus. Hari rayanya dirayakan pada hari Minggu pertama setelah Pentakosta (antara akhir Mei hingga Juni). Persekutuan kasih ini dihayati umat Stasi Rejomulyo dalam membangun kerukunan hidup bersama.',
        address: 'Desa Rejomulyo, Kec. Jati Agung, Kab. Lampung Selatan',
        gmapsUrl: 'https://www.google.com/maps/search/?api=1&query=Gereja+Katolik+Tritunggal+Mahakudus+Rejomulyo+Jati+Agung',
        image: 'assets/img/stasi/rejomulyo.jpg',
        history: '<strong>Asal Usul Desa Rejomulyo (1949–1951)</strong><br>Berdirinya Desa Rejomulyo bermula dari pasangan perantauan dari Jawa Tengah, yaitu Bapak Risman (beragama Islam) dan Ibu Jeane Lavre/Rasmani (beragama Katolik). Awalnya tinggal di Perkebunan Rotterdem (kini PRPN VII Unit Trikora). Pada tahun 1949, akibat situasi perang, keluarga ini bersama 135 pekerja lainnya mengungsi ke hutan pedalaman yang kini dikenal sebagai Rejomulyo. Pada tanggal 8 Januari 1951, Desa Rejomulyo diresmikan oleh negara. Nama Rejomulyo berarti "Makmur". Saat ini desa ini terletak di ujung utara Kecamatan Jati Agung, berbatasan dengan Kecamatan Natar.<br><br><strong>Perkembangan Iman Katolik dan Awal Stasi (1966–1970)</strong><br>Tahun 1966 menjadi cikal bakal berkembangnya Gereja Katolik di Rejomulyo, diawali dengan satu Keluarga Katolik, yaitu keluarga Bapak Sutrisno. Umat bertumbuh pesat akibat dampak pemberontakan PKI. Umat beribadah di rumah Bapak Kepala Desa, didampingi oleh Pastor Vrankeen. Pelayanan Ekaristi awalnya hanya dua bulan sekali. Stasi ini memiliki tradisi unik: sembayangan ibu-ibu setiap Jumat sore dan sembayangan bersama setiap Rabu malam, keduanya masih lestari hingga saat ini.<br><br><strong>Toleransi dan Pembangunan Gereja (1970)</strong><br>Pada tahun 1970, gereja Katolik didirikan bersamaan dengan pembangunan masjid atas iuran bersama warga—sebuah simbol toleransi yang kuat. Kebersamaan ini juga tampak dalam acara gugur gunung (gotong royong) dan pesta HUT RI.<br><br><strong>Statistik Umat (per November 2024)</strong><br>Jumlah umat: 101 jiwa dari 36 Keluarga Katolik (64 jiwa dewasa, 27 jiwa remaja, 10 jiwa anak-anak).',
        chairmen: ['Sutrisno (1966–1980)', 'Sujiman (1981–2000)', 'Dewan Stasi (2001–Sekarang)']
    },
    'jatimulyo': {
        title: 'Stasi Jatimulyo Perdana',
        patron: 'Santo Fransiskus Xaverius',
        patronFeast: '3 Desember',
        patronRole: 'Misionaris Agung & Pelindung Misi Sedunia',
        patronBio: 'Santo Fransiskus Xaverius (1506–1552) adalah imam misionaris Serikat Yesus (Yesuit) dan sahabat Santo Ignatius Loyola. Dikenal sebagai pelindung misi sedunia, ia mengarungi samudera ke India, Kepulauan Maluku Nusantara (Ambon, Ternate, Morotai) pada 1546–1547, hingga Jepang untuk mewartakan Injil dengan semangat berkobar: "Ad Maiorem Dei Gloriam" (Demi Lebih Besarnya Kemuliaan Allah).',
        address: 'Perumahan Jatimulyo Perdana, Kec. Jati Agung, Kab. Lampung Selatan',
        gmapsUrl: 'https://www.google.com/maps/search/?api=1&query=Gereja+Katolik+Santo+Fransiskus+Xaverius+Jatimulyo+Perdana',
        image: 'assets/img/stasi/jatimulyo.jpg',
        history: 'Tahun 1968 menandai awal perintisan Gereja Katolik di wilayah Jatimulyo. Awalnya umat menempuh perjalanan jauh ke Way Kandis atau Kedaton. Umat kemudian bergotong royong membeli tanah di Umbul Niti dan membangun Kapel Santo Ignasius berdinding geribik. Pada 2015, renovasi kapel terhenti karena penolakan warga. Tahun 2016, keluarga F.X. Wakidi dan Agnes Siam menginisiasi gereja baru di Perumahan Jatimulyo Perdana. Selesai tahun 2017, gereja ini diresmikan sebagai stasi pada 2019 oleh Mgr. Yohanes Harun Yuwono. Saat ini terdapat 136 jiwa dari 46 KK.',
        chairmen: ['Nemesius (2019–2022)', 'Andreas Supriono (2022–2023)', 'Harryson Tampubolon (2023–Sekarang)']
    },
    'way-galih': {
        title: 'Stasi Way Galih',
        patron: 'Santo Yohanes De Britto',
        patronFeast: '4 Februari',
        patronRole: 'Misionaris Yesuit & Martir Pembela Kekudusan Iman',
        patronBio: 'Santo Yohanes De Britto (1647–1693) adalah bangsawan Portugis yang menjadi misionaris Yesuit di Madurai, India Selatan. Ia mempraktikkan inkulturasi dengan hidup sebagai pertapa lokal, berpakaian sederhana, dan merangkul kaum kasta terbuang. Keteguhannya membela kekudusan perkawinan Kristiani membuatnya wafat sebagai martir iman yang teguh.',
        address: 'Dusun V.A, Desa Way Galih, Kec. Tanjung Bintang, Kab. Lampung Selatan',
        gmapsUrl: 'https://www.google.com/maps/search/?api=1&query=Gereja+Katolik+Santo+Yohanes+De+Britto+Way+Galih',
        image: 'assets/img/stasi/way-galih.jpg',
        history: 'Terletak di Dusun V.A, Desa Way Galih, di tengah perkebunan PTPN VII. Dirintis tahun 1966 oleh Pastor Vranken. Pada 1968, Bapak Ponijo Harjo Sumarto memelopori pembelian tanah, namun terhalang izin kepala desa. Kapel pertama akhirnya dibangun dari bambu dan atap getepe. Tahun 1975, kerangka kapel kayu dari Sindang Sari dipindah ke Way Galih dan dibangun menjadi semi-permanen dengan bantuan Bapak P. Ngatijo. Tahun 1999, Romo Fransiskus Supomo bersama Bapak Petrus Suharyadi merenovasi total kapel ini, yang kemudian diresmikan pada Natal 1999 dengan pelindung Santo Yohanes De Britto. Saat ini umat mencapai 38 KK (117 jiwa).',
        chairmen: ['Ponijo Harjo Sumarto (1966–?)', 'Ly. Supardi (?–1999)', 'Filipus Marsahid (1999–2001)', 'Paulus Sumardi (2001–2009)', 'Yakobus Maryadi (2009–2015)', 'Andreas Gustami (2015–2018)', 'Fransiskus Xaverius Trimo (2018–Sekarang)']
    },
    'sukadamai': {
        title: 'Stasi Sukadamai',
        patron: 'Santo Yusuf',
        patronFeast: '19 Maret & 1 Mei',
        patronRole: 'Bapa Asuh Yesus & Pelindung Gereja Universal',
        patronBio: 'Santo Yusuf adalah pria tulus hati dan benar di hadapan Allah (iustus), suami Santa Maria, dan bapa asuh Yesus di bumi. Ia melindungi Keluarga Kudus Nazaret dalam ketaatan penuh keheningan kepada Allah. Sebagai pekerja kayu yang tekun, Santo Yusuf menjadi teladan para pekerja, bapa keluarga, serta Pelindung Gereja Universal.',
        address: 'RT 006, Dusun II, Desa Sukadamai, Kec. Natar, Kab. Lampung Selatan',
        gmapsUrl: 'https://www.google.com/maps/search/?api=1&query=Gereja+Katolik+Santo+Yusuf+Sukadamai+Lampung+Selatan',
        image: 'assets/img/stasi/sukadamai.jpg',
        history: '<strong>Asal Usul Umat dan Masa Sulit Perintisan (1960–1972)</strong><br>Komunitas Umat Katolik di Sukadamai tidak lepas dari berdirinya desa tersebut. Warga, termasuk umat Katolik, adalah pendatang dari Lampung Timur dan Lampung Selatan yang bergotong royong membuka lahan—mayoritas berasal dari Marga Jaya. Pada tahun 1960-an, sengketa lahan dengan pemerintah daerah menyebabkan warga harus berpindah ke Way Abung. Banyak umat Katolik merasa kehilangan arah dan tercerai-berai. Meskipun demikian, enam Keluarga Katolik (KK) bertahan di Sukadamai: keluarga Mbah Pantun, Mbah Parto, Mbah Kamso, Mbah Tembeng, Mbah Lasmo, dan Mbah Sukiman.<br><br><strong>Pembangunan Gereja dan Penolakan Sosial (1972)</strong><br>Pada tahun 1972, Romo Vranken membeli sebidang tanah di RT 006, Dusun II, Desa Sukadamai, dan bersama umat mendirikan gereja permanen berbahan batu bata merah—seluruh biayanya ditanggung Romo Vranken. Di tahun yang sama, umat menghadapi penolakan peribadatan dari masyarakat setempat hingga melibatkan aparat desa. Mereka harus bertahan dan melakukan pendekatan hingga tahun 1983, ketika mediasi dengan tokoh agama dan aparat desa berhasil membuahkan kebebasan beribadah. Pembangunan kedua berlangsung pada tahun 2015–2016 melalui gotong royong umat, dengan Romo Hendri dan Romo Roy yang berupaya mendapatkan donatur untuk menyelesaikan pembangunan.<br><br><strong>Dinamika Pelayanan Pastoral dan Perubahan Administrasi (1969–2016)</strong><br>Stasi ini pertama kali masuk karya pastoral Paroki St. Yohanes Rasul, Kedaton pada tahun 1969 (Romo Vranken). Karena jarak yang terlalu jauh, Uskup memindahkan karya pastoral: (1) <em>1985</em>—dipindah ke Paroki Hati Kudus Metro, dilayani Romo Jo, Romo Suroyo, dan Romo Suwar; (2) <em>2005</em>—Paroki Margo Agung berdiri; (3) <em>20 April 2016</em>—Stasi St. Yusuf Sukadamai resmi masuk karya pastoral Paroki Margo Agung (Romo Widarman). Sepanjang perpindahan ini, pelayanan Ekaristi hanya satu bulan sekali, menyebabkan umat mengalami kekeringan rohani yang mendalam.<br><br><strong>Perpecahan Internal dan Isu Multikultural</strong><br>Setelah intoleransi terselesaikan (1983), anggota baru dari suku Batak bergabung. Pada tahun 1987, umat bersuku Jawa mendukung anggota Batak menjadi ketua stasi. Namun perselisihan muncul: umat Batak menginginkan Misa dalam tiga bahasa (Indonesia, Jawa, Batak), sementara umat Jawa hanya menginginkan satu bahasa. Akibatnya, umat bersuku Batak terpaksa berpindah dari Gereja St. Yusuf Sukadamai.<br><br><strong>Penamaan Gereja dan Data Umat</strong><br>Pada tahun 1995 disepakati nama pelindung Santo Yusuf—terinspirasi dari kebijaksanaan Santo Yusuf melindungi keluarganya, sebagai cerminan ketahanan umat menghadapi penolakan. Dari enam keluarga sesepuh di tahun 1960-an, hingga Desember 2025, jumlah umat hanya bertambah menjadi <strong>23 Kepala Keluarga</strong>.',
        chairmen: ['Ketua dari Suku Batak (1987–Tdk Diketahui)', 'Dewan Stasi (Setelah 1987)']
    },
    'pendowo': {
        title: 'Stasi Pendowo',
        patron: 'Santo Aloysius Gonzaga',
        patronFeast: '21 Juni',
        patronRole: 'Pelindung Kaum Muda & Pelajar Katolik',
        patronBio: 'Santo Aloysius Gonzaga (1568–1591) adalah bangsawan muda Italia yang melepaskan takhta demi masuk Serikat Yesus. Dikenal memiliki kesucian hati dan belas kasih mendalam, ia wafat pada usia 23 tahun karena tertular saat dengan sukarela merawat para korban wabah penyakit di Roma. Ia ditetapkan sebagai Pelindung Kaum Muda dan Para Pelajar.',
        address: 'Desa Pancasila (Pendowo), Kec. Natar, Kab. Lampung Selatan',
        gmapsUrl: 'https://maps.app.goo.gl/mhdeNQXPXwW8WJtJ6',
        image: 'assets/img/stasi/pendowo.jpg',
        history: '<strong>Awal Pembukaan Lahan dan Pembentukan Komunitas Umat (1969–1971)</strong><br>Desa Pancasila mulai dibuka sekitar tahun 1969. Pada masa pembukaan hutan, orang-orang berdatangan tanpa memandang latar belakang agama. Kegiatan keagamaan (ulangan/pengajaran) Katolik mulai dilakukan dengan pendampingan Pak Sutrisno (dari Rejomulyo), Simas (dari Metro), dan Jaiman (dari Natar). Umat Katolik asli desa hanya terdiri dari dua KK: keluarga Petrus Samulyo dan Yusuf Sulam—berasal dari Metro, Bedeng 20 Purwodadi, bukan dari program Transmigrasi. Karena sekitar 30 KK tertarik mengikuti pelajaran agama, pada tahun 1971 terjadi baptisan massal, termasuk orang tua narasumber.<br><br><strong>Dinamika Umat dan Status Awal Stasi (1971–1998)</strong><br>Beberapa tahun setelah baptisan massal, banyak dari 30 KK tersebut berpindah tempat. Sekitar tahun 1978–1979, hanya tersisa 15 KK—dan keturunan merekalah yang bertahan hingga saat ini. Jumlah umat kini stagnan, hanya bertambah dari keturunan. Berdasarkan data Biduk 2024, stasi ini memiliki <strong>13 Kepala Keluarga</strong>. Pada masa awal, komunitas ini belum berstatus stasi, melainkan masih berupa kring/lingkungan (ketua: Yakobus Natal). Sejak 1971, lingkungan ini menginduk langsung ke Katedral Kristus Raja (Romo Van Kamp, Romo Stan, Romo Laton, Romo Dwijo), lalu menjadi bagian dari Paroki St. Yohanes Rasul, Kedaton sekitar 1979–1980. Nama Stasi Santo Aloysius, Pendowo mulai dikenal sekitar tahun 1982.<br><br><strong>Sejarah Pembangunan dan Perpindahan Lokasi Gereja</strong><br>Sejak awal (1971), kapel telah dibangun dengan papan. Tanah gereja merupakan jatah pembagian bukaan lahan karena ketua RT/RW kala itu beragama Katolik. Lokasi gereja sempat berpindah akibat permintaan “tukar guling” dari Pak Sumo (warga non-Katolik), dan lahan gereja kini seluas kurang lebih setengah hektar berkat pembelian tambahan oleh Keuskupan dan yayasan pada masa Romo Hendri. Renovasi pertama (1991): kapel papan direhab dengan batu bata, dipimpin Romo Paul dan Romo Totok dari Paroki Kedaton. Administrasi berpindah ke Paroki Margo Agung sekitar 1998. Renovasi kedua/pembangunan total (2013–2014): kusen keropos mendorong rehabilitasi total. Pada <strong>tahun 2014, Gereja St. Aloysius, Stasi Pendowo diberkati oleh Mgr. Yohanes Harun Yuwono</strong>—momen bersejarah karena sejak awal gereja belum pernah diberkati secara resmi. Pemberkatan dihadiri umat dari hampir seluruh stasi di Paroki Margo Agung.',
        chairmen: ['Yakobus Natal (Ketua Kring, Awal)', 'Yuventius Prawoto (1982–...)', 'Pardi', 'Yuventius Prawoto (periode ke-2)', 'Martinus Satimin (1990–...)', 'Yohanes Robert Rebin', 'Markus Ponidi', 'Pardi (periode ke-2)', 'Yohanes Sukiran', 'Albertus Yanto (...–2025)', 'Markus Ruswantoro (2025–Sekarang)']
    },
    'purwotani': {
        title: 'Stasi Purwotani',
        patron: 'Santo Petrus',
        patronFeast: '29 Juni & 22 Februari',
        patronRole: 'Pemimpin Para Rasul & Uskup Roma (Paus) Pertama',
        patronBio: 'Santo Petrus (Simon Petrus) adalah nelayan dari Galilea yang dipanggil Yesus menjadi "penjala manusia" dan batu karang dasar berdirinya Gereja. Sebagai pemimpin para rasul dan Paus pertama, Petrus menunjukkan iman yang bangkit dari kerapuhan menuju kemartiran setia di Roma dengan disalibkan terbalik.',
        address: 'Desa Purwotani, Kec. Jati Agung, Kab. Lampung Selatan',
        gmapsUrl: 'https://www.google.com/maps/search/?api=1&query=Gereja+Katolik+Purwotani+Jati+Agung+Lampung+Selatan',
        image: '',
        history: '<strong>Awal Pembentukan Komunitas dan Perjuangan Toleransi (1984–1990-an)</strong><br>Desa Purwotani terbentuk sekitar tahun 1984. Sebagian besar penduduk, termasuk umat Katolik, adalah pendatang dalam proyek transmigrasi lokal karena bekerja di PT. Midsugoro (Jepang). Umat Katolik perdana berjumpa dan mulai beribadat tahun 1990-an, terdiri dari 8 Keluarga Katolik: Pak Surajan, Pak Albertus Kasimin, Pak Margono, Pak Karinus, Pak Bernardus, Mbah Kasimah, Mbah Petrus Murbadi, dan Mbah Yohanes Saleh. Awalnya merupakan lingkungan di Gereja St. Aloysius Bergen (Paroki Katedral). Pada awal 1990-an, Romo Peco dan katekis Pak Puspayuwono mulai memberikan pelayanan. Umat harus berjuang memperoleh izin dari Kepala Desa dan mendapat pengawasan ibadat karena kecurigaan, tetapi secara perlahan terjalin toleransi yang kuat hingga saat ini tanpa adanya benturan agama.<br><br><strong>Lahan Gereja dan Perpindahan Status Administratif (1992–2022)</strong><br>Sekitar tahun 1992, Romo Peco membeli tanah 3/4 hektar untuk gereja lalu ditukar guling oleh Pak Surajan dengan pekarangan 3 rante di tengah kampung. SKT lahan tersebut sekarang disimpan di Paroki Margo Agung atas nama Supriyanto. Menariknya, hingga saat ini umat belum memiliki bangunan gereja permanen; semua ibadat dan Misa diadakan bergiliran di rumah umat—yang juga berfungsi sebagai kunjungan pastoral langsung. Pada 2022, lingkungan tersebut pindah ke Paroki Margo Agung dan baru resmi ditetapkan sebagai <strong>Stasi Santo Petrus Purwotani</strong> oleh Romo Philipus Suroyo.<br><br><strong>Semangat Beribadat dan Data Umat Terkini</strong><br>Sebelumnya Misa hanya diterima pada Minggu ke-5 dan kini umat sering bergabung ke Stasi Marga Lestari di Hari Minggu. Semangat umat sangat tinggi, terbukti dengan partisipasi utuh saat ada ibadah lokal di stasi. Saat ini, jumlah umat Stasi Purwotani tercatat ada <strong>7 Kepala Keluarga (25 jiwa)</strong>.',
        chairmen: ['Surajan (Ketua Lingkungan, 1990–?)', 'Sempat ada kekosongan ketua', 'Albertus Kasimin (Ketua Lingkungan, ?–2021)', 'Benediktus Daman (Ketua Stasi, 2022–Sekarang)']
    }
};

// Map aliases to guarantee all 9 stasi keys work under any naming convention
stasiData['marga-agung']   = stasiData['margo-agung'];
stasiData['margoagung']    = stasiData['margo-agung'];
stasiData['marga-lestari'] = stasiData['marga-lestari'];
stasiData['margalestari']  = stasiData['marga-lestari'];
stasiData['sindang-sari']  = stasiData['sindang-sari'];
stasiData['sindangsari']   = stasiData['sindang-sari'];
stasiData['way-galih']     = stasiData['way-galih'];
stasiData['waygalih']      = stasiData['way-galih'];
stasiData['sinar-rejeki']  = stasiData['rejomulyo'];
stasiData['sinarrejeki']   = stasiData['rejomulyo'];
stasiData['budi-lestari']  = stasiData['purwotani'];
stasiData['budilestari']   = stasiData['purwotani'];

function showAlert(title, message, type = 'success') {
    const overlay = document.createElement('div');
    overlay.className = 'fixed inset-0 z-[100] flex items-center justify-center p-4 bg-black/60 backdrop-blur-sm opacity-0 transition-opacity duration-300';
    
    const isError = type === 'error';
    const iconColor = isError ? 'bg-red-100 text-red-600' : 'bg-green-100 text-green-600';
    const iconStr = isError ? '✖' : '✔';
    const btnColor = isError ? 'bg-red-600 hover:bg-red-700' : 'bg-green-600 hover:bg-green-700';

    overlay.innerHTML = `
        <div class="bg-white rounded-3xl p-8 max-w-sm w-full text-center shadow-2xl transform scale-90 transition-transform duration-300" id="alertBox">
            <div class="${iconColor} w-20 h-20 mx-auto mb-4 rounded-full flex items-center justify-center text-4xl shadow-inner font-bold">
                ${iconStr}
            </div>
            <h3 class="text-2xl font-bold mb-2 text-gray-800">${title}</h3>
            <p class="text-gray-600 mb-6">${message}</p>
            <button id="closeAlertBtn" class="${btnColor} text-white font-bold py-3 px-8 rounded-xl w-full transition shadow-md hover:shadow-lg transform hover:-translate-y-0.5">Tutup</button>
        </div>
    `;
    
    document.body.appendChild(overlay);

    requestAnimationFrame(() => {
        overlay.classList.remove('opacity-0');
        overlay.querySelector('#alertBox').classList.remove('scale-90');
        overlay.querySelector('#alertBox').classList.add('scale-100');
    });

    const closeBtn = overlay.querySelector('#closeAlertBtn');
    const closeAlert = () => {
        overlay.classList.add('opacity-0');
        overlay.querySelector('#alertBox').classList.remove('scale-100');
        overlay.querySelector('#alertBox').classList.add('scale-90');
        setTimeout(() => overlay.remove(), 300);
    };

    closeBtn.addEventListener('click', closeAlert);
    overlay.addEventListener('click', (e) => {
        if (e.target === overlay) closeAlert();
    });
}

function showToast(message, type = 'success') {
    const existing = document.getElementById('global-toast');
    if (existing) existing.remove();

    const toast = document.createElement('div');
    toast.id = 'global-toast';
    const isError = type === 'error';
    const bgColor = isError ? 'bg-[#DC2626] text-white border border-red-400/40 shadow-red-900/40' : 'bg-[#101012] text-amber-300 border border-amber-500/40 shadow-black/50';
    const iconHtml = isError 
        ? `<span class="text-base flex-shrink-0 font-bold cursor-pointer hover:opacity-70 transition-opacity" onclick="this.parentElement.style.display='none'" title="Tutup">✕</span>` 
        : `<span class="text-base flex-shrink-0 font-bold">✓</span>`;
    
    toast.className = `fixed bottom-6 left-1/2 -translate-x-1/2 max-w-[92vw] md:max-w-md w-max px-5 py-3.5 rounded-2xl font-semibold shadow-2xl backdrop-blur-md transform transition-all duration-300 translate-y-16 opacity-0 z-[999999] flex items-center gap-3 text-sm ${bgColor}`;
    toast.innerHTML = `${iconHtml}<span class="leading-snug text-center">${message}</span>`;
    
    document.body.appendChild(toast);

    requestAnimationFrame(() => {
        toast.classList.remove('translate-y-16', 'opacity-0');
        toast.classList.add('translate-y-0', 'opacity-100');
    });

    setTimeout(() => {
        if(document.body.contains(toast)) {
            toast.classList.remove('translate-y-0', 'opacity-100');
            toast.classList.add('translate-y-16', 'opacity-0');
            setTimeout(() => toast.remove(), 320);
        }
    }, 3800);
}

function ensureStasiModalInDOM() {
    let modal = document.getElementById('stasiModal');
    if (!modal) {
        modal = document.createElement('div');
        modal.id = 'stasiModal';
        modal.className = 'fixed inset-0 z-[60] hidden items-center justify-center p-3 md:p-6';
        modal.style.setProperty('display', 'none', 'important');
        modal.style.setProperty('pointer-events', 'none', 'important');
        modal.style.background = 'rgba(28,28,30,0.8)';
        modal.innerHTML = `
            <div class="modal-card w-full max-w-4xl max-h-[90vh] md:max-h-[88vh] relative flex flex-col overflow-hidden">
                <button class="modal-close-btn absolute top-4 right-4 md:top-5 md:right-5 z-50 w-10 h-10 rounded-full flex items-center justify-center text-xl font-bold" aria-label="Tutup modal">✕</button>
                <div id="modalContent" class="p-6 md:p-10 overflow-y-auto custom-scrollbar flex-grow min-h-0"></div>
            </div>
        `;
        document.body.appendChild(modal);

        modal.querySelector('.modal-close-btn').addEventListener('click', closeStasiModal);
        modal.addEventListener('click', (e) => {
            if (e.target === modal) closeStasiModal();
        });
    }
    return modal;
}

let _globalModalScrollPos = 0;
let _openModalCount = 0;

function lockBodyScroll() {
    if (_openModalCount === 0) {
        _globalModalScrollPos = window.pageYOffset || document.documentElement.scrollTop || window.scrollY || 0;
        document.body.classList.add('modal-open');
        document.documentElement.classList.add('modal-open');
        document.body.style.overflow = 'hidden';
    }
    _openModalCount++;
}

function unlockBodyScroll() {
    _openModalCount = Math.max(0, _openModalCount - 1);
    if (_openModalCount === 0) {
        document.body.classList.remove('modal-open');
        document.documentElement.classList.remove('modal-open');
        document.body.style.overflow = '';
        window.scrollTo({ top: _globalModalScrollPos, behavior: 'instant' });
    }
}

function openStasiModal(id) {
    const cleanId = String(id || '').trim().toLowerCase().replace(/_/g, '-');
    const data = stasiData[cleanId] || stasiData[id];
    
    if (!data) {
        showToast('Informasi untuk stasi ini belum tersedia.', 'error');
        return;
    }

    const modal = ensureStasiModalInDOM();
    const content = document.getElementById('modalContent');
    
    const chairmenList = data.chairmen.map((name, i) =>
        `<li class="flex items-center gap-2 py-1.5 border-b text-sm" style="border-color:var(--border);color:var(--ink-soft)">
            <span class="w-5 h-5 rounded flex-shrink-0 flex items-center justify-center text-xs font-bold" style="background:var(--gold-muted);color:var(--gold)">${i+1}</span>
            <span>${name}</span>
         </li>`
    ).join('');

    let imageHtml = '';
    if (data.image) {
        const safeTitle = (data.title || '').replace(/'/g, "\\'");
        const safePatron = (data.patron || '').replace(/'/g, "\\'");
        imageHtml = `
        <div class="relative w-full rounded-2xl overflow-hidden shadow-lg mb-6 flex items-center justify-center cursor-pointer group border transition-all hover:shadow-xl" 
             style="max-height:440px; min-height:220px; background:#141416; border-color:var(--border);"
             onclick="openLightbox('${data.image}', '${safeTitle}', {title:'${safeTitle}', keterangan:'Gedung Gereja / Stasi ${safeTitle} · Pelindung: ${safePatron}', badge:'Gereja &amp; Stasi'})"
             title="Klik untuk melihat foto penuh">
            
            <!-- Ambient blurred backdrop to seamlessly frame church photo -->
            <img src="${data.image}" alt="" class="absolute inset-0 w-full h-full object-cover filter blur-xl opacity-35 scale-110 pointer-events-none" aria-hidden="true">
            
            <!-- Full uncropped church building photo -->
            <img src="${data.image}" alt="${data.title}" class="relative z-10 max-h-[380px] md:max-h-[440px] w-auto max-w-full object-contain rounded-xl transition-transform duration-300 group-hover:scale-[1.02] shadow-sm">
            
            <!-- Zoom Hint Badge -->
            <div class="absolute bottom-3 right-3 z-20 px-3.5 py-1.5 rounded-full text-xs font-semibold text-white flex items-center gap-1.5 shadow-md transition-all group-hover:shadow-lg" 
                 style="background:rgba(28,28,30,0.8);backdrop-filter:blur(8px);border:1px solid rgba(255,255,255,0.2)">
                <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"></circle><line x1="21" y1="21" x2="16.65" y2="16.65"></line><line x1="11" y1="8" x2="11" y2="14"></line><line x1="8" y1="11" x2="14" y2="11"></line></svg>
                <span>Lihat Foto Penuh</span>
            </div>
        </div>`;
    } else {
        imageHtml = `
        <div class="w-full rounded-2xl mb-6 p-8 flex flex-col items-center justify-center text-center" style="background:var(--gold-muted);border:1.5px dashed var(--gold)">
            <span class="text-3xl font-bold mb-1.5" style="color:var(--gold)">✝</span>
            <span class="font-bold text-base" style="color:var(--ink)">Gereja Dalam Pembangunan</span>
            <span class="text-xs text-gray-500 mt-1">Ibadat & Misa bergiliran di rumah umat stasi</span>
        </div>`;
    }

    content.innerHTML = `
        <div class="flex flex-wrap items-center justify-between gap-3 mb-4 pr-8">
            <div>
                <div class="inline-flex items-center gap-1.5 text-xs font-bold uppercase tracking-wider px-3 py-1 rounded-full mb-2" style="background:var(--gold-muted);color:var(--gold)">
                    <span>✝</span> Pelindung: ${data.patron}
                </div>
                <h2 class="text-2xl md:text-3xl font-bold leading-tight" style="color:var(--ink)">${data.title}</h2>
            </div>
            ${data.gmapsUrl ? `
                <a href="${data.gmapsUrl}" target="_blank" rel="noopener noreferrer" class="btn-primary inline-flex items-center gap-2 text-xs py-2 px-4 shadow-sm">
                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg> Buka Google Maps
                </a>
            ` : ''}
        </div>

        ${imageHtml}

        <!-- Patron Box -->
        <div class="p-5 md:p-6 rounded-2xl mb-6" style="background:rgba(201,168,76,0.08);border:1px solid rgba(201,168,76,0.25)">
            <div class="flex flex-wrap items-center justify-between gap-2.5 mb-2.5">
                <div class="text-sm md:text-base font-bold flex items-center gap-1.5" style="color:var(--gold)">
                    <span>✝</span> Mengenal Pelindung: ${data.patron}
                </div>
                ${data.patronFeast ? `<span class="inline-flex items-center text-[11px] md:text-xs px-3 py-1 rounded-full font-bold whitespace-nowrap flex-shrink-0 tracking-wide" style="background:var(--gold-muted);color:var(--gold);border:1px solid rgba(201,168,76,0.25)">Pesta: ${data.patronFeast}</span>` : ''}
            </div>
            <div class="text-xs font-semibold mb-2" style="color:var(--ink)">${data.patronRole || ''}</div>
            <p class="text-xs md:text-sm leading-relaxed text-gray-600" style="text-align:justify">${data.patronBio || ''}</p>
        </div>

        <!-- Address Bar -->
        ${data.address ? `
            <div class="flex items-start gap-2.5 text-xs text-gray-600 mb-6 p-3.5 rounded-xl" style="background:var(--bg-alt);border:1px solid var(--border)">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="flex-shrink-0 mt-0.5" style="color:var(--gold)"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx="12" cy="10" r="3"></circle></svg>
                <div class="flex-grow">
                    <strong style="color:var(--ink)">Alamat:</strong> ${data.address}
                </div>
                <a href="${data.gmapsUrl}" target="_blank" rel="noopener noreferrer" class="text-xs font-bold hover:underline flex-shrink-0" style="color:var(--gold)">Petunjuk Arah ↗</a>
            </div>
        ` : ''}

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-8">
            <div class="md:col-span-2">
                <div class="flex items-center gap-2 mb-3">
                    <div class="w-1 h-5 rounded-full" style="background:var(--gold)"></div>
                    <h4 class="font-bold text-base" style="color:var(--ink)">Sejarah Stasi</h4>
                </div>
                <div class="text-gray-600 leading-relaxed text-sm" style="text-align:justify">${data.history}</div>
            </div>
            <div>
                <div class="sticky top-4 p-5 rounded-2xl" style="background:var(--bg-alt);border:1px solid var(--border)">
                    <div class="flex items-center gap-2 mb-3">
                        <div class="w-1 h-5 rounded-full" style="background:var(--gold)"></div>
                        <h4 class="font-bold text-sm" style="color:var(--ink)">Daftar Ketua Stasi</h4>
                    </div>
                    <ul class="max-h-64 overflow-y-auto custom-scrollbar space-y-1">
                        ${chairmenList}
                    </ul>
                </div>
            </div>
        </div>
    `;
    
    modal.classList.remove('hidden');
    modal.classList.add('flex');
    modal.style.setProperty('display', 'flex', 'important');
    modal.style.setProperty('pointer-events', 'auto', 'important');
    lockBodyScroll();

    if (window.history && window.history.pushState) {
        window.history.pushState({ modal: 'stasiModal', id: cleanId }, '');
    }
}

function closeStasiModal(fromPopState = false) {
    const modal = document.getElementById('stasiModal');
    if (!modal) return;
    modal.classList.add('hidden');
    modal.classList.remove('flex');
    modal.style.setProperty('display', 'none', 'important');
    modal.style.setProperty('pointer-events', 'none', 'important');
    unlockBodyScroll();

    if (!fromPopState && window.history && history.state && history.state.modal === 'stasiModal') {
        window.history.back();
    }
}

// ── Lightbox Functions ───────────────────────────────────────
let currentLightboxIndex = 0;
let lightboxImages = [];

function ensureLightboxInDOM() {
    let lb = document.getElementById('lightbox');
    if (!lb) {
        lb = document.createElement('div');
        lb.id = 'lightbox';
        lb.style.setProperty('display', 'none', 'important');
        lb.style.setProperty('pointer-events', 'none', 'important');
        lb.innerHTML = `
            <button class="lb-close" title="Tutup">&times;</button>
            <button class="lb-prev" title="Sebelumnya">&#10094;</button>
            <button class="lb-next" title="Selanjutnya">&#10095;</button>
            <div id="lightbox-content" style="max-width:min(92vw, 950px);width:100%;text-align:center;margin:0 auto;position:relative;">
                <img id="lightbox-img" src="" alt="" style="max-height:85vh;max-width:100%;display:inline-block;object-fit:contain;border-radius:1rem;box-shadow:0 24px 64px rgba(0,0,0,0.6)">
                <div id="lightbox-info-box" class="px-5 py-4 rounded-2xl w-full" style="position:absolute;bottom:0;left:50%;transform:translate(-50%, 50%);max-width:90%;background:rgba(28,28,30,0.85);backdrop-filter:blur(16px);border:1px solid rgba(255,255,255,0.12);z-index:10001;display:flex;flex-direction:column;align-items:center;">
                    <div class="flex items-center justify-center gap-2 mb-1.5">
                        <span id="lightbox-badge" class="text-[10px] font-bold uppercase tracking-wider px-2.5 py-0.5 rounded-full text-white" style="display:none"></span>
                        <span id="lightbox-date" class="text-xs text-amber-400 font-medium" style="display:none"></span>
                    </div>
                    <h4 id="lightbox-title" class="text-white text-sm md:text-base font-bold leading-snug m-0 text-center"></h4>
                    <p id="lightbox-desc" class="text-gray-300 text-[11px] md:text-xs mt-1 leading-relaxed m-0 text-center" style="display:none"></p>
                </div>
            </div>
            <div id="lightbox-counter"></div>
        `;
        document.body.appendChild(lb);

        lb.querySelector('.lb-close').addEventListener('click', closeLightbox);
        lb.querySelector('.lb-prev').addEventListener('click', (e) => { e.stopPropagation(); changeLightboxImage(-1); });
        lb.querySelector('.lb-next').addEventListener('click', (e) => { e.stopPropagation(); changeLightboxImage(1); });
        lb.addEventListener('click', handleLightboxClick);

        let startX = 0;
        lb.addEventListener('touchstart', e => { startX = e.touches[0].clientX; }, { passive: true });
        lb.addEventListener('touchend', e => {
            const diff = e.changedTouches[0].clientX - startX;
            if (Math.abs(diff) > 50) changeLightboxImage(diff < 0 ? 1 : -1);
        }, { passive: true });
    }
    return lb;
}

function buildLightboxImages() {
    // Only select gallery items that are NOT hidden (i.e. not filtered out)
    const cards = Array.from(document.querySelectorAll('.gallery-item, .gallery-card, [data-lightbox]'))
        .filter(card => window.getComputedStyle(card).display !== 'none');

    lightboxImages = cards.map(item => {
        const img = item.querySelector('img') || (item.tagName === 'IMG' ? item : null);
        const titleEl = item.querySelector('.gallery-title, h3, h4');
        const descEl = item.querySelector('.gallery-desc, .gallery-sub, p');
        const dateEl = item.querySelector('.gallery-date');
        const badgeEl = item.querySelector('.gallery-badge');
        
        return {
            src: img ? (img.currentSrc || img.src || img.getAttribute('src')) : '',
            rawSrc: img ? (img.getAttribute('src') || '') : '',
            alt: img ? img.alt : '',
            title: titleEl ? titleEl.textContent.trim() : (img ? img.alt : ''),
            keterangan: descEl ? descEl.textContent.trim() : '',
            date: dateEl ? dateEl.textContent.trim() : '',
            badge: badgeEl ? badgeEl.textContent.trim() : '',
            badgeBg: badgeEl ? (badgeEl.style.backgroundColor || badgeEl.style.background) : ''
        };
    }).filter(item => item.src);
}

function openLightbox(src, alt, customData = null) {
    const lb = ensureLightboxInDOM();
    if (customData && customData.single) {
        lightboxImages = [{
            src: src,
            alt: alt || 'Foto',
            title: (customData && customData.title) || alt || 'Foto',
            keterangan: (customData && customData.keterangan) || '',
            badge: (customData && customData.badge) || 'STASI',
            badgeBg: (customData && customData.badgeBg) || 'var(--gold)',
            date: (customData && customData.date) || ''
        }];
        currentLightboxIndex = 0;
    } else {
        buildLightboxImages();
        let idx = lightboxImages.findIndex(i => i.src === src || (i.rawSrc && src && (src.endsWith(i.rawSrc) || i.rawSrc.endsWith(src))));
        if (idx < 0) {
            if (src) {
                lightboxImages.push({
                    src: src,
                    alt: alt || 'Foto',
                    title: (customData && customData.title) || alt || 'Foto',
                    keterangan: (customData && customData.keterangan) || '',
                    badge: (customData && customData.badge) || 'GALERI',
                    badgeBg: (customData && customData.badgeBg) || 'var(--gold)',
                    date: (customData && customData.date) || ''
                });
                idx = lightboxImages.length - 1;
            } else {
                idx = 0;
            }
        }
        currentLightboxIndex = idx >= 0 ? idx : 0;
    }
    renderLightbox();

    if (lb) {
        lb.classList.remove('hidden');
        lb.classList.add('active');
        lb.style.setProperty('display', 'flex', 'important');
        lb.style.setProperty('pointer-events', 'auto', 'important');
        lockBodyScroll();

        if (window.history && window.history.pushState) {
            window.history.pushState({ modal: 'lightbox' }, '');
        }
    }
}

function closeLightbox(fromPopState = false) {
    const lb = document.getElementById('lightbox');
    if (lb) {
        lb.classList.add('closing');
        setTimeout(() => {
            lb.classList.remove('active', 'closing');
            lb.style.setProperty('display', 'none', 'important');
            lb.style.setProperty('pointer-events', 'none', 'important');
            unlockBodyScroll();
        }, 200);
    }
    if (!fromPopState && window.history && history.state && history.state.modal === 'lightbox') {
        window.history.back();
    }
}

function handleLightboxClick(e) {
    if (e.target === document.getElementById('lightbox')) {
        closeLightbox();
    }
}

function renderLightbox() {
    const data = lightboxImages[currentLightboxIndex];
    if (!data) return;

    const img = document.getElementById('lightbox-img');
    const titleEl = document.getElementById('lightbox-title');
    const descEl = document.getElementById('lightbox-desc');
    const badgeEl = document.getElementById('lightbox-badge');
    const dateEl = document.getElementById('lightbox-date');
    const counter = document.getElementById('lightbox-counter');
    const prevBtn = document.querySelector('.lb-prev');
    const nextBtn = document.querySelector('.lb-next');

    if (img) {
        img.src = data.src;
        img.alt = data.title || data.alt;
    }
    if (titleEl) titleEl.textContent = data.title || data.alt;

    if (descEl) {
        if (data.keterangan && data.keterangan !== data.title) {
            descEl.textContent = data.keterangan;
            descEl.style.display = 'block';
        } else {
            descEl.style.display = 'none';
        }
    }

    if (badgeEl) {
        if (data.badge) {
            badgeEl.textContent = data.badge;
            if (data.badgeBg) badgeEl.style.background = data.badgeBg;
            badgeEl.style.display = 'inline-block';
        } else {
            badgeEl.style.display = 'none';
        }
    }

    if (dateEl) {
        if (data.date) {
            dateEl.textContent = data.date;
            dateEl.style.display = 'inline-block';
        } else {
            dateEl.style.display = 'none';
        }
    }

    const hasMultiple = lightboxImages.length > 1;
    if (prevBtn) prevBtn.style.display = hasMultiple ? 'flex' : 'none';
    if (nextBtn) nextBtn.style.display = hasMultiple ? 'flex' : 'none';
    if (counter) {
        counter.style.display = hasMultiple ? 'block' : 'none';
        if (hasMultiple) counter.textContent = `${currentLightboxIndex + 1} / ${lightboxImages.length}`;
    }
}

function changeLightboxImage(dir) {
    if (lightboxImages.length <= 1) return;
    const img = document.getElementById('lightbox-img');

    if (img) {
        img.classList.add('changing');
        setTimeout(() => {
            currentLightboxIndex = (currentLightboxIndex + dir + lightboxImages.length) % lightboxImages.length;
            renderLightbox();
            img.classList.remove('changing');
        }, 150);
    }
}

// ── Global Keyboard Shortcuts ────────────────────────────────
document.addEventListener('keydown', e => {
    // Lightbox handling
    const lb = document.getElementById('lightbox');
    if (lb && lb.classList.contains('active')) {
        if (e.key === 'ArrowRight') changeLightboxImage(1);
        if (e.key === 'ArrowLeft')  changeLightboxImage(-1);
        if (e.key === 'Escape')     closeLightbox();
        return;
    }

    // Modal handling
    const modal = document.getElementById('stasiModal');
    if (modal && !modal.classList.contains('hidden') && e.key === 'Escape') {
        closeStasiModal();
    }
});

// ── Gallery Initializer & Filtering ──────────────────────────
window.currentGalleryFilter = 'all';

window.filterGallery = function(filterCategory, btnElement) {
    const rawFilter = (filterCategory || 'all').toLowerCase().trim();
    window.currentGalleryFilter = rawFilter;

    // Update filter button states
    const btns = document.querySelectorAll('.filter-btn');
    btns.forEach(btn => {
        const bFilter = (btn.getAttribute('data-filter') || '').toLowerCase().trim();
        const isActive = (btnElement && btn === btnElement) || (bFilter === rawFilter);
        if (isActive) {
            btn.classList.add('active-filter');
            btn.setAttribute('aria-pressed', 'true');
        } else {
            btn.classList.remove('active-filter');
            btn.setAttribute('aria-pressed', 'false');
        }
    });

    // Filter gallery cards
    const items = document.querySelectorAll('.gallery-item, .gallery-card');
    let visibleCount = 0;

    items.forEach(item => {
        const cat = (item.getAttribute('data-category') || item.dataset.category || '').toLowerCase().trim();
        let show = false;

        if (rawFilter === 'all') {
            show = true;
        } else if (cat === rawFilter) {
            show = true;
        } else if (rawFilter === 'omk' && cat.includes('omk')) {
            show = true;
        } else if (rawFilter === 'liturgi' && (cat.includes('liturgi') || cat.includes('misa'))) {
            show = true;
        } else if (rawFilter === 'perayaan' && (cat.includes('perayaan') || cat.includes('natal') || cat.includes('paskah'))) {
            show = true;
        } else if (rawFilter === 'sosial' && (cat.includes('sosial') || cat.includes('lingkungan'))) {
            show = true;
        }

        item.style.display = show ? '' : 'none';
        if (show) visibleCount++;
    });

    // Toggle empty state message
    const emptyMsg = document.getElementById('empty-msg');
    if (emptyMsg) {
        if (visibleCount === 0) {
            emptyMsg.classList.remove('hidden');
            emptyMsg.style.display = 'block';
        } else {
            emptyMsg.classList.add('hidden');
            emptyMsg.style.display = 'none';
        }
    }
};

function setupFilters() {
    const btns = document.querySelectorAll('.filter-btn');
    if (!btns.length) return;

    btns.forEach(btn => {
        btn.onclick = function(e) {
            e.preventDefault();
            const filter = this.getAttribute('data-filter') || 'all';
            window.filterGallery(filter, this);
        };
    });
}

function setupGalleryClicks() {
    document.querySelectorAll('.gallery-item, .gallery-card').forEach(item => {
        item.style.cursor = 'pointer';
    });
}

// Global click delegation for all interactive elements (supports dynamic & static content)
document.addEventListener('click', (e) => {
    // 1. If clicked on a filter button, filter gallery
    const filterBtn = e.target.closest('.filter-btn, [data-filter]');
    if (filterBtn) {
        const filter = filterBtn.getAttribute('data-filter') || 'all';
        window.filterGallery(filter, filterBtn);
        return;
    }

    // 2. If clicked on stasi card or button
    const stasiEl = e.target.closest('[data-stasi]');
    if (stasiEl) {
        e.preventDefault();
        const stasiId = stasiEl.getAttribute('data-stasi');
        if (stasiId) {
            openStasiModal(stasiId);
        }
        return;
    }

    // 3. If clicked on a gallery card/item, open lightbox
    const card = e.target.closest('.gallery-item, .gallery-card, [data-lightbox]');
    if (card) {
        // If clicking a link or button inside the card, let it proceed
        if (e.target.closest('button:not(.filter-btn), a')) return;
        const img = card.querySelector('img') || (card.tagName === 'IMG' ? card : null);
        if (img) {
            e.preventDefault();
            const src = img.currentSrc || img.src || img.getAttribute('src');
            const alt = img.alt || '';
            openLightbox(src, alt);
        }
    }
});

// ── Scroll To Top Initializer ────────────────────────────────
function ensureScrollTopInDOM() {
    let scrollBtn = document.getElementById('scroll-top');
    if (!scrollBtn) {
        scrollBtn = document.createElement('button');
        scrollBtn.id = 'scroll-top';
        scrollBtn.setAttribute('aria-label', 'Kembali ke atas');
        scrollBtn.textContent = '↑';
        document.body.appendChild(scrollBtn);
    }
    scrollBtn.addEventListener('click', () => {
        window.scrollTo({ top: 0, behavior: 'smooth' });
    });
    window.addEventListener('scroll', () => {
        if (window.scrollY > 300) scrollBtn.classList.add('visible');
        else scrollBtn.classList.remove('visible');
    }, { passive: true });
}

function getGreetingText() {
    const hour = new Date().getHours();
    if (hour >= 5  && hour < 12) return 'Selamat Pagi';
    if (hour >= 12 && hour < 15) return 'Selamat Siang';
    if (hour >= 15 && hour < 18) return 'Selamat Sore';
    return 'Selamat Malam';
}

function updateGreetings() {
    const greetingText = getGreetingText();
    const heroGreeting = document.getElementById('greeting-hero');
    const oldGreeting  = document.getElementById('greeting');
    if (heroGreeting) heroGreeting.textContent = `${greetingText} — Berkah Dalem`;
    if (oldGreeting)  oldGreeting.textContent  = `${greetingText}, Berkah Dalem.`;
}

function setTextContentById(id, value) {
    const el = document.getElementById(id);
    if (!el || value === undefined || value === null) return;
    el.innerHTML = String(value);
}

function setAttrById(id, attr, value) {
    const el = document.getElementById(id);
    if (!el || value === undefined || value === null || value === '') return;
    el.setAttribute(attr, String(value));
}

function renderProfileLeaderList(items) {
    const container = document.getElementById('profile-leaders-list');
    if (!container) return;

    const list = Array.isArray(items) ? items.filter(Boolean) : [];
    container.innerHTML = '';

    list.forEach((name, index) => {
        const row = document.createElement('div');
        row.className = 'flex items-center gap-3 p-3 rounded-xl hover:bg-black/5 transition-colors';

        const number = document.createElement('div');
        number.className = 'w-8 h-8 rounded-lg flex items-center justify-center text-sm font-bold flex-shrink-0';
        number.style.background = 'var(--gold-muted)';
        number.style.color = 'var(--gold)';
        number.textContent = String(index + 1);

        const label = document.createElement('span');
        label.className = 'text-sm';
        if (index === list.length - 1) label.classList.add('font-semibold');
        label.style.color = 'var(--ink)';
        label.textContent = name;

        row.appendChild(number);
        row.appendChild(label);
        container.appendChild(row);
    });
}

function applyHomeSiteContent(content) {
    const home = content?.home || {};
    const hero = home.hero || {};

    setTextContentById('hero-badge', hero.badge);
    setTextContentById('hero-line-1', hero.titleLines?.[0]);
    setTextContentById('hero-line-2', hero.titleLines?.[1]);
    setTextContentById('hero-line-3', hero.titleLines?.[2]);
    setTextContentById('hero-subtitle', hero.subtitle);
    setTextContentById('hero-cta-primary-text', hero.primaryCta?.text);
    setAttrById('hero-cta-primary', 'href', hero.primaryCta?.href);
    setTextContentById('hero-cta-secondary-text', hero.secondaryCta?.text);
    setAttrById('hero-cta-secondary', 'href', hero.secondaryCta?.href);

    (home.stats || []).slice(0, 4).forEach((stat, index) => {
        setTextContentById(`home-stat-${index + 1}-value`, stat.value);
        setTextContentById(`home-stat-${index + 1}-label`, stat.label);
    });

    const announcements = home.announcements || {};
    setTextContentById('home-announcement-badge', announcements.badge);
    setTextContentById('home-announcement-title', announcements.title);
    setTextContentById('home-announcement-subtitle', announcements.subtitle);
    setTextContentById('home-announcement-card-title', announcements.cardTitle);
    setTextContentById('home-renungan-label', announcements.devotionLabel);

    const schedule = home.schedule || {};
    setTextContentById('home-schedule-badge', schedule.badge);
    setTextContentById('home-schedule-title', schedule.title);
    setTextContentById('home-schedule-subtitle', schedule.subtitle);
    setTextContentById('home-schedule-link-text', schedule.linkText);

    const gallery = home.gallery || {};
    setTextContentById('home-gallery-badge', gallery.badge);
    setTextContentById('home-gallery-title', gallery.title);
    setTextContentById('home-gallery-link', gallery.linkText);

    const stasi = home.stasi || {};
    setTextContentById('home-stasi-badge', stasi.badge);
    setTextContentById('home-stasi-title', stasi.title);
    setTextContentById('home-stasi-subtitle', stasi.subtitle);
    setTextContentById('home-stasi-link', stasi.linkText);
}

function applyProfileSiteContent(content) {
    const profile = content?.profile || {};
    const organization = profile.organization || {};
    const visionMission = profile.visionMission || {};
    const saints = profile.saints || {};
    const history = profile.history || {};
    const leaders = profile.leaders || {};

    setTextContentById('profile-hero-title', profile.hero?.title);
    setTextContentById('profile-hero-subtitle', profile.hero?.subtitle);

    setTextContentById('profile-org-badge', organization.badge);
    setTextContentById('profile-org-title', organization.title);
    setTextContentById('profile-org-subtitle', organization.subtitle);
    setTextContentById('profile-paroki-heading', organization.parokiHeading);
    setTextContentById('profile-paroki-text', organization.parokiText);
    setTextContentById('profile-stasi-heading', organization.stasiHeading);
    setTextContentById('profile-stasi-text', organization.stasiText);

    setTextContentById('profile-vision-badge', visionMission.badge);
    setTextContentById('profile-vision-title', visionMission.title);
    setTextContentById('profile-vision-subtitle', visionMission.subtitle);
    setTextContentById('profile-vision-label', visionMission.visionLabel);
    setTextContentById('profile-vision-heading', visionMission.visionTitle);
    setTextContentById('profile-vision-text', visionMission.visionText);
    setTextContentById('profile-mission-label', visionMission.missionLabel);
    setTextContentById('profile-mission-heading', visionMission.missionTitle);
    (visionMission.missionItems || []).slice(0, 3).forEach((item, index) => setTextContentById(`profile-mission-item-${index + 1}`, item));

    setTextContentById('profile-saints-badge', saints.badge);
    setTextContentById('profile-saints-title', saints.title);
    setTextContentById('profile-saints-subtitle', saints.subtitle);

    setTextContentById('profile-history-badge', history.badge);
    setTextContentById('profile-history-title', history.title);
    setTextContentById('profile-history-subtitle', history.subtitle);
    (history.items || []).slice(0, 3).forEach((item, index) => {
        setTextContentById(`profile-history-year-${index + 1}`, item.year);
        setTextContentById(`profile-history-title-${index + 1}`, item.title);
        setTextContentById(`profile-history-text-${index + 1}`, item.text);
    });

    setTextContentById('profile-leaders-badge', leaders.badge);
    setTextContentById('profile-leaders-title', leaders.title);
    setTextContentById('profile-leaders-subtitle', leaders.subtitle);
    
    let chairmenList = leaders.items;
    if (typeof stasiData !== 'undefined' && stasiData['margo-agung'] && Array.isArray(stasiData['margo-agung'].chairmen) && stasiData['margo-agung'].chairmen.length > 0) {
        chairmenList = stasiData['margo-agung'].chairmen;
    }
    renderProfileLeaderList(chairmenList);
}

function applyContactSiteContent(content) {
    const contact = content?.contact || {};
    const quick = contact.quickActions || {};
    const sekretariat = contact.sekretariat || {};
    const form = contact.form || {};
    const map = contact.map || {};

    setTextContentById('contact-hero-badge', contact.hero?.badge);
    setTextContentById('contact-hero-title', contact.hero?.title);
    setTextContentById('contact-hero-subtitle', contact.hero?.subtitle);

    const phoneStr = sekretariat.phone || '(0721) 755300';
    let numeric = String(phoneStr).trim().replace(/[^\d+]/g, '');
    if (numeric.startsWith('0')) {
        numeric = '62' + numeric.substring(1);
    } else if (numeric.startsWith('+62')) {
        numeric = '62' + numeric.substring(3);
    }
    const waLink = `https://wa.me/${numeric}`;
    const callLink = `tel:${numeric.startsWith('62') ? '0' + numeric.substring(2) : numeric}`;

    setTextContentById('contact-wa-text', 'WhatsApp Sekretariat');
    setAttrById('contact-wa-link', 'href', waLink);
    setTextContentById('contact-phone-text', `Telepon: ${phoneStr}`);
    setAttrById('contact-phone-link', 'href', callLink);

    setTextContentById('contact-sekretariat-title', sekretariat.title);
    setTextContentById('contact-address', sekretariat.address);
    setTextContentById('contact-phone', sekretariat.phone);
    setTextContentById('contact-email', sekretariat.email);
    setTextContentById('contact-hours-weekday', sekretariat.hoursWeekday);
    setTextContentById('contact-hours-weekend', sekretariat.hoursWeekend);

    setTextContentById('contact-form-title', form.title);
    setTextContentById('contact-form-subtitle', form.subtitle);
    setTextContentById('contact-submit-text', form.submitText);

    setTextContentById('contact-map-badge', map.badge);
    setTextContentById('contact-map-title', map.title);
    setTextContentById('contact-map-subtitle', map.subtitle);
    setTextContentById('contact-map-address-label', map.addressLabel);
    setTextContentById('contact-map-address-line', map.addressLine);
    setTextContentById('contact-map-button', map.buttonText);
    setAttrById('contact-map-button', 'href', map.mapUrl);
    setAttrById('contact-map-iframe', 'src', map.iframeUrl);

    const defaultFaq = (window.SA_SITE_CONTENT_DEFAULTS && window.SA_SITE_CONTENT_DEFAULTS.contact && window.SA_SITE_CONTENT_DEFAULTS.contact.faq) || [];
    const faqList = contact.faq || [];
    faqList.slice(0, 4).forEach((item, index) => {
        const def = defaultFaq[index] || {};
        setTextContentById(`contact-faq-q-${index + 1}`, item.question || def.question);
        setTextContentById(`contact-faq-a-${index + 1}`, item.answer || def.answer);
    });
}

function applyFooterSiteContent(content) {
    const brand = content?.brand || {};
    const contact = content?.contact || {};
    const sekretariat = contact.sekretariat || {};

    setTextContentById('footer-brand-name', brand.name);
    setTextContentById('footer-brand-subtitle', brand.subtitle);
    setTextContentById('footer-description', brand.description);
    setAttrById('footer-instagram', 'href', brand.instagram);
    setAttrById('footer-youtube', 'href', brand.youtube);

    setTextContentById('footer-address', sekretariat.address);
    setTextContentById('footer-phone', sekretariat.phone);
    setTextContentById('footer-email', sekretariat.email);
    setTextContentById('footer-email', sekretariat.email);
    setAttrById('footer-email', 'href', sekretariat.email ? `mailto:${sekretariat.email}` : '#');
    setTextContentById('footer-hours', (sekretariat.hoursWeekday && sekretariat.hoursWeekend) 
        ? `${sekretariat.hoursWeekday.replace('Senin – Jumat', 'Sen–Jum')}<br>${sekretariat.hoursWeekend.replace('Sabtu – Minggu', 'Sab–Min')}`
        : '');
    setTextContentById('footer-copyright', brand.copyright);
    setTextContentById('footer-tagline', brand.tagline);
}

function applySiteContentToPage(content) {
    const pathname = (location.pathname || '').toLowerCase();
    
    // Always apply footer
    applyFooterSiteContent(content);

    if (pathname.endsWith('index.html') || pathname.endsWith('/') || pathname === '') {
        applyHomeSiteContent(content);
    }
    if (pathname.endsWith('profil.html') || pathname.endsWith('/profil')) {
        applyProfileSiteContent(content);
    }
    if (pathname.endsWith('kontak.html') || pathname.endsWith('/kontak')) {
        applyContactSiteContent(content);
    }
}

async function refreshSiteContentOnPage() {
    try {
        const content = typeof window.loadSiteContent === 'function'
            ? await window.loadSiteContent()
            : (typeof window.getSiteContent === 'function' ? window.getSiteContent() : null);
        if (content) applySiteContentToPage(content);
    } catch (e) {
        console.warn('Apply site content note:', e);
    }
}

// Execute immediately if DOM elements already parsed
if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', updateGreetings);
} else {
    updateGreetings();
}

// ── Sync Stasi Data from Supabase ────────────────────────────
async function syncStasiFromSupabase() {
    // 1. First sync from localStorage cache if available
    try {
        const cached = localStorage.getItem('saint_andrew_stasi_cache');
        if (cached) {
            const parsed = JSON.parse(cached);
            Object.values(parsed).forEach(item => {
                if (stasiData[item.id]) {
                    if (item.nama) stasiData[item.id].title = item.nama;
                    if (item.pelindung) stasiData[item.id].patron = item.pelindung;
                    if (item.pesta_nama) stasiData[item.id].patronFeast = item.pesta_nama;
                    if (item.role || item.peran_pelindung) stasiData[item.id].patronRole = item.role || item.peran_pelindung;
                    if (item.biografi_pelindung) stasiData[item.id].patronBio = item.biografi_pelindung;
                    if (item.alamat) stasiData[item.id].address = item.alamat;
                    if (item.gmaps_url) stasiData[item.id].gmapsUrl = item.gmaps_url;
                    if (item.foto_url) stasiData[item.id].image = item.foto_url;
                    if (item.sejarah) stasiData[item.id].history = item.sejarah;
                }
            });
        }
    } catch (e) {}

    // 2. Fetch live data from Supabase
    if (typeof db === 'undefined') return;
    try {
        const { data, error } = await db.from('stasi').select('*');
        if (!error && data && data.length > 0) {
            data.forEach(item => {
                if (stasiData[item.id]) {
                    if (item.nama) stasiData[item.id].title = item.nama;
                    if (item.pelindung) stasiData[item.id].patron = item.pelindung;
                    if (item.pesta_nama) stasiData[item.id].patronFeast = item.pesta_nama;
                    if (item.role || item.peran_pelindung) stasiData[item.id].patronRole = item.role || item.peran_pelindung;
                    if (item.biografi_pelindung) stasiData[item.id].patronBio = item.biografi_pelindung;
                    if (item.alamat) stasiData[item.id].address = item.alamat;
                    if (item.gmaps_url) stasiData[item.id].gmapsUrl = item.gmaps_url;
                    if (item.foto_url) stasiData[item.id].image = item.foto_url;
                    if (item.sejarah) stasiData[item.id].history = item.sejarah;
                    
                    let parsedChairmen = [];
                    if (Array.isArray(item.daftar_ketua)) {
                        parsedChairmen = item.daftar_ketua;
                    } else if (typeof item.daftar_ketua === 'string' && item.daftar_ketua.trim() !== '') {
                        try {
                            parsedChairmen = JSON.parse(item.daftar_ketua);
                            if (!Array.isArray(parsedChairmen)) parsedChairmen = [item.daftar_ketua];
                        } catch(e) {
                            parsedChairmen = item.daftar_ketua.replace(/^\{|\}$/g, '').split(',').map(s => s.replace(/^"|"$/g, '').trim()).filter(Boolean);
                        }
                    }
                    if (parsedChairmen.length > 0) {
                        stasiData[item.id].chairmen = parsedChairmen;
                    }
                }
            });
            updateStasiCardsDOM();
        }
    } catch (e) {
        console.warn('Sync stasi error:', e);
    }
}

async function syncHomeScheduleFromSupabase() {
    const grid = document.getElementById('home-schedule-grid');
    if (!grid) return;
    
    if (typeof db === 'undefined') return;
    try {
        const { data, error } = await db.from('jadwal_misa')
            .select('*')
            .eq('lokasi', 'margo-agung')
            .order('urutan')
            .limit(3);
            
        if (!error && data && data.length > 0) {
            grid.innerHTML = data.map(item => `
                <div class="info-card text-center">
                  <div class="w-12 h-12 mx-auto mb-3 rounded-2xl flex items-center justify-center text-lg font-bold" style="background:var(--gold-muted);color:var(--gold)">✝</div>
                  <div class="text-xs font-bold uppercase tracking-widest mb-1" style="color:var(--gold)">${item.hari}</div>
                  <div class="text-2xl font-black" style="color:var(--ink)">${item.jam}</div>
                  <div class="text-xs mt-1 font-medium" style="color:var(--stone)">WIB · ${item.keterangan || 'Misa Umat'}</div>
                </div>
            `).join('');
        }
    } catch (e) {
        console.warn('Sync home schedule error:', e);
    }
}
function updateStasiCardsDOM() {
    const cards = document.querySelectorAll('[data-stasi]');
    cards.forEach(card => {
        const id = card.getAttribute('data-stasi');
        const data = stasiData[id];
        if (!data) return;

        const titleEl = card.querySelector('h3.font-bold, div.font-bold');
        const patronEl = card.querySelector('p.font-semibold, div.font-medium');

        if (titleEl && data.title) titleEl.textContent = data.title;
        if (patronEl && data.patron) patronEl.textContent = data.patron;
    });
}


async function syncHomeGalleryFromSupabase() {
    const container = document.getElementById('gallery-container');
    if (!container) return;
    
    if (typeof db === 'undefined') return;
    try {
        const { data, error } = await db.from('galeri')
            .select('*')
            .order('urutan')
            .order('created_at', { ascending: false })
            .limit(10);
            
        if (!error && data && data.length > 0) {
            container.innerHTML = data.map(item => `
                <div class="gallery-item flex-none w-72 snap-center group" style="aspect-ratio:4/3">
                  <img src="${item.foto_url || 'assets/img/placeholder.jpg'}" alt="${item.judul}" class="w-full h-full object-cover">
                  <div class="gallery-overlay">
                    <span class="gallery-title">${item.judul}</span>
                    <span class="gallery-sub">${item.keterangan || item.kategori}</span>
                  </div>
                </div>
            `).join('');
        } else {
            container.innerHTML = '<div class="text-sm text-gray-500 italic p-4">Belum ada foto kegiatan.</div>';
        }
    } catch (e) {
        console.warn('Sync home gallery error:', e);
    }
}

// ── Main DOM Ready Initializer ───────────────────────────────
document.addEventListener('DOMContentLoaded', async () => {
    // 0. Sync dynamic Stasi from DB
    await syncStasiFromSupabase();


    // 0a. Sync home schedule
    await syncHomeScheduleFromSupabase();

    // 0b. Sync home gallery
    await syncHomeGalleryFromSupabase();


    // 0b. Load editable site content for public pages
    await refreshSiteContentOnPage();

    // 1. Setup Scroll-to-top
    ensureScrollTopInDOM();

    // 2. Setup Gallery & Lightbox triggers
    setupGalleryClicks();
    setupFilters();

    // 2b. Scroll Reveal — IntersectionObserver
    // Make above-fold elements visible immediately, rest on scroll
    const revealObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('visible');
                revealObserver.unobserve(entry.target); // fire once
            }
        });
    }, { threshold: 0.08, rootMargin: '0px 0px -20px 0px' });

    document.querySelectorAll('.reveal, .reveal-left, .reveal-right').forEach(el => {
        // If already in viewport (above the fold), mark visible immediately
        const rect = el.getBoundingClientRect();
        if (rect.top < window.innerHeight) {
            el.classList.add('visible');
        } else {
            revealObserver.observe(el);
        }
    });

    // 3. Update greeting once more to ensure all elements caught
    updateGreetings();

    // 5. Contact form submission & validation (Supabase integrated)
    const contactForm = document.getElementById('contactForm');
    if (contactForm) {
        contactForm.addEventListener('submit', async function(e) {
            e.preventDefault();
            const btn      = contactForm.querySelector('button[type="submit"]');
            const textEl   = document.getElementById('submitText');
            const alertBox = document.getElementById('contact-alert');
            const origText = textEl ? textEl.textContent : (btn ? btn.textContent : 'Kirim Pesan');

            const nameEl    = document.getElementById('cf-name') || document.getElementById('name');
            const emailEl   = document.getElementById('cf-email') || document.getElementById('email');
            const teleponEl = document.getElementById('cf-telepon') || document.getElementById('telepon');
            const subjekEl  = document.getElementById('cf-subjek') || document.getElementById('subject');
            const pesanEl   = document.getElementById('cf-pesan') || document.getElementById('message');

            const name    = nameEl?.value.trim() || '';
            const email   = emailEl?.value.trim() || '';
            const telepon = teleponEl?.value.trim() || '';
            const subject = subjekEl?.value || '';
            const message = pesanEl?.value.trim() || '';

            const missing = [];
            const missingEls = [];
            if (!name)    { missing.push('Nama Lengkap'); if (nameEl) missingEls.push(nameEl); }
            if (!email)   { missing.push('Email');        if (emailEl) missingEls.push(emailEl); }
            if (!subject) { missing.push('Subjek');       if (subjekEl) missingEls.push(subjekEl); }
            if (!message) { missing.push('Pesan');        if (pesanEl) missingEls.push(pesanEl); }

            if (missing.length > 0) {
                // Highlight empty inputs with dynamic clearing on user interaction
                missingEls.forEach(el => {
                    el.classList.add('input-error');
                    const cleanHandler = () => {
                        el.classList.remove('input-error');
                        el.removeEventListener('input', cleanHandler);
                        el.removeEventListener('change', cleanHandler);
                    };
                    el.addEventListener('input', cleanHandler);
                    el.addEventListener('change', cleanHandler);
                });

                if (alertBox) {
                    alertBox.className = 'mb-5 flex items-center gap-2.5 p-3.5 rounded-xl text-sm font-medium bg-red-50 text-red-700 border border-red-200';
                    alertBox.innerHTML = `<span class="flex-shrink-0 text-base font-bold cursor-pointer hover:text-red-900 transition-colors" onclick="this.parentElement.style.display='none'" title="Tutup">✕</span><div class="flex-grow leading-relaxed">Mohon lengkapi kolom wajib: <strong class="font-bold text-red-800">${missing.join(', ')}</strong></div>`;
                    alertBox.style.display = 'flex';
                    
                    // Smoothly scroll into view so mobile user sees it instantly
                    alertBox.scrollIntoView({ behavior: 'smooth', block: 'center' });
                }

                // Focus first missing input
                if (missingEls.length > 0) {
                    missingEls[0].focus({ preventScroll: true });
                }

                // Show instant floating toast on viewport (pops up immediately on screen!)
                showToast(`Mohon lengkapi: ${missing.join(', ')}`, 'error');
                return;
            }

            if (textEl) textEl.textContent = 'Mengirim pesan…';
            if (btn) btn.disabled = true;
            if (alertBox) alertBox.style.display = 'none';

            try {
                if (typeof db !== 'undefined' && db) {
                    const payload = {
                        nama:       name,
                        email:      email,
                        telepon:    telepon || null,
                        subjek:     subject,
                        pesan:      message,
                        created_at: new Date().toISOString()
                    };

                    const { error } = await db.from('pesan').insert([payload]);
                    if (error) throw error;
                }

                showAlert(
                    'Pesan Berhasil Terkirim!',
                    `Terima kasih <strong>${name}</strong>. Pesan Anda telah kami terima dan akan segera ditindaklanjuti oleh sekretariat paroki.`,
                    'success'
                );
                contactForm.reset();
            } catch (err) {
                console.error('Gagal mengirim pesan kontak:', err);
                // Give specific, actionable error messages per error code
                let errMsg;
                if (err?.code === '42P01') {
                    errMsg = 'Tabel pesan belum disiapkan di database. Hubungi administrator.';
                } else if (err?.code === '42501' || err?.message?.includes('row-level security') || err?.message?.includes('security policy')) {
                    errMsg = 'Konfigurasi izin database belum lengkap. Hubungi administrator untuk mengaktifkan RLS policy tabel "pesan".';
                } else {
                    errMsg = err?.message || 'Terjadi kesalahan jaringan. Coba lagi.';
                }
                showAlert('Gagal Mengirim', errMsg, 'error');
            } finally {
                if (textEl) textEl.textContent = origText;
                if (btn) btn.disabled = false;
            }
        });
    }

    // 6. FAQ Accordion
    document.querySelectorAll('.faq-question').forEach(q => {
        q.addEventListener('click', () => {
            const item = q.closest('.faq-item');
            if (!item) return;
            const isOpen = item.classList.contains('open');
            document.querySelectorAll('.faq-item.open').forEach(el => el.classList.remove('open'));
            if (!isOpen) item.classList.add('open');
        });
    });

    // 7. Scroll reveal animation (IntersectionObserver)
    if ('IntersectionObserver' in window) {
        const revealObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                    revealObserver.unobserve(entry.target);
                }
            });
        }, { threshold: 0.05, rootMargin: '0px 0px -10px 0px' });

        document.querySelectorAll('.reveal, .reveal-left, .reveal-right').forEach(el => {
            revealObserver.observe(el);
        });
    } else {
        // Fallback for older browsers
        document.querySelectorAll('.reveal, .reveal-left, .reveal-right').forEach(el => {
            el.classList.add('visible');
        });
    }
});

// ── Global Window Exports for public interaction ─────────────
window.openStasiModal = openStasiModal;
window.closeStasiModal = closeStasiModal;
window.openLightbox = openLightbox;
window.closeLightbox = closeLightbox;
window.changeLightboxImage = changeLightboxImage;
window.filterGallery = filterGallery;

// ── Web Component: <aesthetic-divider> ──
if (!customElements.get('aesthetic-divider')) {
    customElements.define('aesthetic-divider', class extends HTMLElement {
        connectedCallback() {
            const isReveal = this.hasAttribute('reveal') ? ' reveal' : '';
            const customStyle = this.getAttribute('style') || '';
            const extraClass = this.getAttribute('class') || '';
            this.innerHTML = `
            <div class="aesthetic-divider${isReveal} ${extraClass}" style="${customStyle}">
                <div class="divider-line"></div>
                <div class="divider-icon">✝</div>
                <div class="divider-line"></div>
            </div>`;
        }
    });
}

// Global helper function for JS-driven templates
if (!window.renderAestheticDivider) {
    window.renderAestheticDivider = function(extraClass = '', customStyle = '') {
        const styleAttr = customStyle ? ` style="${customStyle}"` : '';
        return `<div class="aesthetic-divider ${extraClass}"${styleAttr}><div class="divider-line"></div><div class="divider-icon">✝</div><div class="divider-line"></div></div>`;
    };
}